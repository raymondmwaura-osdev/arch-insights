# `<sub-command>` e.g. `commit`

## Overview

Provide a concise description of the sub-command’s primary function.

Explain in what scenarios the sub-command is typically employed and how it fits within the broader workflow of the main command.

Highlight the effect of executing the sub-command, including its default behavior and any noteworthy operational characteristics.

---

## Syntax

Present the canonical syntax of the sub-command, showing its structural pattern.

Identify the essential elements required for the sub-command to execute successfully.

Note optional elements that affect the sub-command’s execution or outcome (exclude detailed switches/flags).

---

## Switches

Document each switch supported by the sub-command.

For each switch, specify its short and long form (if applicable), whether it is optional or required, and its default behavior.

Describe the operational impact of the switch, including how it modifies or augments the sub-command’s execution.

Call out any constraints, incompatibilities, or interactions with other switches.

---

## Operational Notes

Specify any preconditions, dependencies, or environmental factors influencing the sub-command.

Discuss any ordering or sequencing implications relative to other sub-commands.

Summarize what the user sees or the system state changes after execution.

---

## Example

Provide a single, representative example illustrating the sub-command’s basic usage, emphasizing structure over specifics.

---
