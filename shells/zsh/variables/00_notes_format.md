## `<variable name>` e.g `PROMPT`

A precise description of what the variable represents and its role in zsh’s execution or user interface model.

Document the default setting.

## Data Type and Structure

Specify whether the variable is a scalar, array, associative array, or special parameter, and describe any syntactic constraints.

### Expansion and Evaluation Semantics

Explain how and when the variable is expanded (e.g., prompt expansion, parameter expansion flags, runtime vs. parse-time behavior).

### Configuration and Customization

Common configuration patterns, including typical assignments, modifiers, or related options (`setopt` / `unsetopt`).

### Interactions and Dependencies

Notable interactions with other variables, shell options, or built-in mechanisms.

### Examples

One or two canonical examples demonstrating practical usage.

---
