# `steam`

Use `steam -shutdown` to close steam. This ensures that steam closes well and all pending tasks like writing to disk are finalized. Forcing steam to close can cause problems like progress not being saved properly in games.

---
